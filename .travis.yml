language: python

before-install:
  - pip install -r requirements.txt

script:
  - python manage.py migrate
  - black .
  - flake8 . --ignore=F401 accounts.signals
  - coverage run --source='.' manage.py test

after_success:
  - coveralls